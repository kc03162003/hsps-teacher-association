/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/hsps-teacher-association',
  trailingSlash: true,
  /* config options here */
};

export default nextConfig;
